# Sao chép thành .env cạnh docker-compose.infra.yml (hoặc export trước khi compose).
# docker compose --env-file .env -f ops/docker-compose.infra.yml up -d

# Public host/IP của máy DigitalOcean để client bên ngoài connect Kafka.
# Ví dụ: 203.0.113.10 hoặc kafka.tenmien.com
DO_PUBLIC_HOST=127.0.0.1

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=doi-mat-khau-manh-o-day
